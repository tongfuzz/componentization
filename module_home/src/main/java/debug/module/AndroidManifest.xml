<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joyukc.home">

    <application
        android:name=".HomeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/home_ic_launcher"
        android:label="@string/home_app_name"
        android:roundIcon="@mipmap/home_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ysx">
        <activity
            android:name="debug.HomeTestActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>